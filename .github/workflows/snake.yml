name: GitHub Contribution Snake

on:
  workflow_dispatch:

jobs:
  snake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false  # important for using GITHUB_TOKEN

      - name: Create dist folder
        run: mkdir -p dist

      - name: Generate Snake
        uses: Platane/snk@v3
        with:
          github_user_name: kimbi12
          outputs: |
            dist/github-contribution-grid-snake.svg

      - name: Configure Git
        run: |
          git config user.name "Kimbi Denis"
          git config user.email "kimbidenis67@gmail.com"

      - name: Commit & Push SVG
        run: |
          git add dist/github-contribution-grid-snake.svg
          git diff --cached --quiet || git commit -m "Update snake"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/kimbi12/kimbi12.git HEAD:main
